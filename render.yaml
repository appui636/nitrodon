services:
  - type: web
    name: introdone
    env: node
    buildCommand: echo "No build needed"
    startCommand: node src/server.js
    envVars:
      - key: PORT
        value: 10000
    disk:
      name: music
      mountPath: /music
      sizeGB: 1
